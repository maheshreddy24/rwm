seed: 42
device: cuda:0

# model params copied verbatim from configs/train_ema.yaml
model:
  encoder_name: facebook/dinov2-small
  core_layers: 4
  core_heads: 8
  dec_dim: 512
  dec_layers: 3
  dec_heads: 16
  dec_mlp: 768
  mask_ratio: 1
  max_delta: 64
  freeze_encoder: True
  checkpoint_core: false

dataset_config_path: /home/rvm/ablations/ssv2_probe/dataset_config.yaml
# pretrained RVM (torch) checkpoint to load into the frozen backbone -- paste your path here
rvm_weights_path: /home/rvm/ablations/ssv2_probe/ckpts_jlabs/small_dec.pth

lr: 3.0e-4
weight_decay: 1.0e-4
betas: [0.9, 0.999]
eps: 1.0e-8

num_epochs: 5
warmup_ratio: 0.05
min_lr: 1.0e-7

batch_size: 128
num_workers: 16
amp: true

log_interval: 200
eval_interval: 400
checkpoint_dir: ckpts/ssv2_meanpool_probe_torch

wandb_project: rvm-action-recog
wandb_name: ssv2-meanpool-probe-torch

num_classes: 174
