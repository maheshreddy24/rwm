seed: 42

model:
  encoder_name: facebook/dinov2-small
  core_layers: 4
  core_heads: 8
  dec_dim: 512
  dec_layers: 3
  dec_heads: 16
  dec_mlp: 768
  mask_ratio: 0.95
  max_delta: 64
  freeze_encoder: false
  checkpoint_core: false

dataset:
  train:
    video_csv: /home/rvm/datasets/train_dataset_ssv2_kine.csv
    num_source_frames: 4
    num_target_frames: 4
    max_delta: 48
    frame_size: [256, 256]   # must match encoder patch_size (14) so H/patch is an integer grid
    min_duration_sec: 4
  eval:
    video_csv: /home/rvm/datasets/test_dataset_ssv2_kine.csv
    num_source_frames: 4
    num_target_frames: 4
    max_delta: 48
    frame_size: [256, 256]
    min_duration_sec: 4

dataloader:
  batch_size: 128
  num_workers: 16
  pin_memory: true

trainer:
  checkpoint_dir: checkpoints_dino_backbone_frozen
  lr: 1.5e-4          # recurrent core + decoder + repr_head
  weight_decay: 0.05
  betas: [0.9, 0.95]
  eps: 1e-8
  grad_clip_norm: 1.0

  num_epochs: 5
  warmup_ratio: 0.05
  min_lr_ratio: 0.01

  normalize_target: true

  save_every_steps: 1000
  eval_every_steps: 1000

  amp: true
  log_interval: 100

  wandb_project: rvm_dino
  wandb_name: null
